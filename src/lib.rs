// ============================================================
// AIL Runtime — библиотечная грань крейта (Проход 2).
//
// Зачем: у бинарного крейта (main.rs) нет lib-цели, поэтому вторые бинарники
// (src/bin/*) и интеграционные тесты не могут импортировать его типы — ровно
// об это спотыкался integration_test.rs из лога Google-Search_2026_07_22
// (use ail_runtime::{...} из крейта без lib.rs не компилируется).
//
// Здесь публикуются только САМОДОСТАТОЧНЫЕ модули (без зависимостей от
// остального дерева main.rs): их исходники компилируются и в lib, и в bin.
// ============================================================

pub mod shard_engine;
// self_healing живёт только в bin-крейте (main.rs): он зависит от
// compiler::{lexer,parser,verifier}, которые объявлены внутри main-дерева.
