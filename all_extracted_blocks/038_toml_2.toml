[package]
name = "ail_binary_runtime"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3" # Сверхбыстрая бинарная сериализация для Rust