[package]
name = "ail_actor_runtime"
version = "0.3.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
crossbeam-channel = "0.5" # Атомарные lock-free каналы